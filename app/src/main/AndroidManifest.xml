<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meetingrooms4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".RoomsActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".OpenRoomsActivity" />
        <activity android:name=".RoomsFragment" />
        <activity android:name=".DateFragment" />
        <activity android:name=".AllBookingsFragment" />
        <activity android:name=".BookNowFragment" />

        <receiver android:name=".NotificationReceiver" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>